import OpsSidebar from './OpsSidebar';

/**
 * Neste app a navegação principal é a sidebar do console ops.
 * `Navbar` é mantido como alias estável para os page agents.
 */
export default function Navbar() {
  return <OpsSidebar />;
}
